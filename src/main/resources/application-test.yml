spring:
  application:
    name: Bank Service - Test
  datasource:
    url: jdbc:h2:mem:bankdb_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

security:
  jwt:
    secret: c2VjcmV0LWtleS1taW4tMzItY2hhcnMtc2VjcmV0LXNob3VsZC1iZS1sb25n

server:
  port: 18080

app:
  self-base-url: http://localhost:18080
